{
  "name": "magicbox-proxy",
  "version": "1.3.0",
  "description": "Reverse proxy for MagicBox — routes API and dynamic WebSocket streams",
  "main": "src/index.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "test": "node --test --test-timeout=60000 test/"
  },
  "dependencies": {
    "http-proxy": "^1.18.1",
    "ws": "^8.18.0"
  }
}
