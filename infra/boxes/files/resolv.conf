# MANAGED BY infra/boxes/files/resolv.conf — deployed by deploy.sh.
# NetworkManager is told dns=none (NetworkManager/conf.d/90-attila-dns.conf), so
# this file is ours. Two independent anycast resolvers; the vendor default put
# 114.114.114.114 (China) first, 250 ms away from a box in Paris.
nameserver 1.1.1.1
nameserver 8.8.8.8
options timeout:2 attempts:2
