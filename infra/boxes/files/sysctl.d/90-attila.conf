# MANAGED BY infra/boxes/files/sysctl.d/90-attila.conf — deployed by deploy.sh.
#
# box-2/3/4 (firmware 2.0.x) boot with vm.swappiness=10 and stay there. box-1
# (firmware E1.02, CBS 1.1.6.12.1) was measured at 100 on 25 Sep 2026 with 8
# containers up: zram 4 GB at 100 %, kswapd0 at 95 % CPU, load average 192.
# The vendor's zram-init.service asserts 10 at boot; something raised it later
# (cbs_go 1.1.6.x carries an undocumented /swappiness/set route). This file
# pins the fleet value; check-drift reports the RUNTIME value so a re-flip is
# seen, not assumed away.
vm.swappiness = 10
